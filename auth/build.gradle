plugins {
    id 'java'
    id "com.github.johnrengelman.shadow" version "8.1.1"
    id 'net.minecrell.plugin-yml.bukkit' version '0.6.0'
}

group 'com.minecraft.lobby'
version '1.5.7-SNAPSHOT'

compileJava.options.encoding = 'UTF-8'

repositories {
    mavenCentral()
    maven { url "https://hub.spigotmc.org/nexus/content/groups/public/" }
}

dependencies {
    compile project(':core-bukkit')

    compileOnly 'org.projectlombok:lombok:1.18.22'
    annotationProcessor 'org.projectlombok:lombok:1.18.8'

    compileOnly group: 'redis.clients', name: 'jedis', version: '3.1.0'

    compileOnly files("C:\\Libraries\\bukkit-server.jar")
    compileOnly files("C:\\Libraries\\ViaVersion.jar")
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

jar {
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}

bukkit {
    name = 'Auth'
    main = 'com.minecraft.auth.Auth'
    version = '1.5.7-' + gitVersion()
    website = 'www.ateumc.com'
    description = 'Auth bukkit plugin based on core, core-bukkit and third based on core-proxy.'
}

static String gitVersion() {
    return 'git rev-parse --verify --short HEAD'.execute().getText().trim()
}

def shadowJarTask = tasks.getByName("shadowJar")

tasks.register("Ateu", Copy) {
    dependsOn shadowJarTask
    from shadowJarTask.outputs.files
    into "/home/NesquikDeveloper/AteuMC/updater"
}

shadowJar {
    dependencies {
        include(project(':core-bukkit'))
    }
    archiveClassifier = null
}